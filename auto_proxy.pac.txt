function FindProxyForURL(url, host) {
  if (isInNet(myIpAddress(), "140.88.95.0", "255.255.255.0"))
    return "DIRECT";
  else if (dnsDomainIs(host, ".bethel.edu") || 
        shExpMatch(url, "*.bethel.edu"))
    return "SOCKS5 localhost:3900";
  else
    return "DIRECT";
}